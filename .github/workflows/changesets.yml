name: Changesets

on:
  push:
    branches: [main]

permissions:
  id-token: write
  contents: write
  pull-requests: write
  packages: write

jobs:
  changesets:
    uses: coldsurfers/workflows/.github/workflows/changesets-for-packages.yml@main
    with:
      repository: 'coldsurfers/surfers-common'
    secrets:
      npm_token: ${{ secrets.GITHUB_TOKEN }}
